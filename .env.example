# ============================================================================
# ПОЛНЫЙ .ENV ФАЙЛ ДЛЯ YANDEX CLOUD + MINIKUBE + ARGOCD
# ============================================================================
# Все переменные с рекомендуемыми значениями для fid-team.ru
# Заполните реальные значения для ваших параметров

# ============================================================================
# РАЗДЕЛ 1: YANDEX CLOUD АУТЕНТИФИКАЦИЯ
# ============================================================================
# Выберите один из способов:
# Вариант 1: OAuth Token (рекомендуется)
YC_OAUTH_TOKEN=y0_AgA...  # Ваш OAuth токен отсюда: https://oauth.yandex.ru/authorize?response_type=token&client_id=c0e3b0ff61d94fb29eae

# ID облака (найти в https://console.cloud.yandex.ru)
YC_CLOUD_ID=b1gjqd8ioc0ctf8vp67c

# ID каталога (папки) в облаке
YC_FOLDER_ID=b1gg2878t6o5k2d0r96c

# Зона расположения ВМ (Доступные)
# ru-central1-a;
# ru-central1-b;
# ru-central1-d — рекомендуемая зона для новых проектов;
# ru-central1-m — отдельная зона сервиса Yandex BareMetal.

YC_ZONE=ru-central1-a

# ============================================================================
# РАЗДЕЛ 2: SSH КЛЮЧИ
# ============================================================================
# Путь к публичному SSH ключу (для доступа на ВМ)
SSH_PUBLIC_KEY=~/.ssh/id_rsa.pub

# Путь к приватному SSH ключу (для Ansible подключения)
ANSIBLE_SSH_PRIVATE_KEY=~/.ssh/id_rsa

# ============================================================================
# РАЗДЕЛ 3: ПАРАМЕТРЫ ВИРТУАЛЬНОЙ МАШИНЫ
# ============================================================================
# Имя ВМ (видно в консоли Yandex Cloud)
VM_NAME=yc-minikube-argocd

# Пользователь на ВМ (для Ubuntu образов - ubuntu)
VM_USERNAME=ubuntu

# ID образа ОС (если оставить пусто, используется Ubuntu 24.04 LTS)
IMAGE_ID=

# Количество виртуальных процессоров (минимум 4 для Minikube)
VM_CORES=4

# Объем оперативной памяти в ГБ (минимум 8 для Minikube)
VM_MEMORY=8

# Размер загрузочного диска в ГБ (минимум 30, рекомендуется 50)
VM_DISK_SIZE=50

# ============================================================================
# РАЗДЕЛ 4: ДОМЕН И DNS
# ============================================================================
# Корневое доменное имя
DOMAIN=fid-team.ru

# Поддомен для ArgoCD
ARGOCD_SUBDOMAIN=argocd

# ============================================================================
# РАЗДЕЛ 5: SSL СЕРТИФИКАТЫ И EMAIL
# ============================================================================
# Email для выпуска Let's Encrypt сертификатов
# Это ОБЯЗАТЕЛЬНО! Используется для восстановления сертификата
EMAIL=admin@fid-team.ru

# ============================================================================
# РАЗДЕЛ 6: KUBERNETES ВЕРСИЯ
# ============================================================================
# Версия kubectl (если оставить пусто - используется последняя стабильная)
KUBECTL_VERSION=v1.28.0

# ============================================================================
# РАЗДЕЛ 7: TERRAFORM (локальное состояние)
# ============================================================================
# Путь к директории с состоянием Terraform
TF_STATE_PATH=./infra/terraform


# ============================================================================
# РАЗДЕЛ 8: ФИЧА-ФЛАГИ (TOGGLES)
# ============================================================================
# Переключатели для включения/отключения компонентов установки
# true/false (регистронезависимо)
# Использовать Let's Encrypt Staging (true/false)
LETSENCRYPT_STAGING=false
# Устанавливать ingress-nginx
ENABLE_INGRESS_NGINX=true
# Устанавливать cert-manager
ENABLE_CERT_MANAGER=true
# Создавать ClusterIssuer (staging/prod по LETSENCRYPT_STAGING)
ENABLE_CLUSTER_ISSUER=true
# Устанавливать Argo CD
ENABLE_ARGOCD=true
# Создавать Ingress для Argo CD
ENABLE_ARGOCD_INGRESS=true
# Устанавливать Harbor (Docker Registry)
ENABLE_HARBOR=false

# Поддомен реестра (используется при ENABLE_HARBOR=true)
REGISTRY_SUBDOMAIN=registry
